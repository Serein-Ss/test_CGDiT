# 执行记录：2026-09-14

## 已执行

1. 研究提案与审计资料迁入本目录；复制数据快照和历史指标日志并保存哈希清单。
2. 整理 1284 条记录，优先核查 188 条，确定 30 条试点。这里的温度仍是原始标签，不是已核验的居里温度。
3. 官方 Intern API 模型列表探测成功，`intern-s1` 可用。
4. 完成 5 次实际提取请求，HTTP 均为 200，返回模型名 `Intern-S1`；服务返回总 token 数合计 1372。这不是费用或免费额度承诺。
5. 4 个文献案例覆盖 6 条试点记录；另用 1 条无来源记录测试拒绝推断。全部通过 JSON 格式和引文匹配检查，无来源案例返回空事件。30 条试点中仍有 24 条未完成来源收集。
6. 针对实际错误增加保守审查标记，重复处理使用缓存，不额外请求 API。

## 实际发现及处理

| 案例 | 模型输出 | 本次审查 |
|---|---|---|
| NdScO3 | 0.953 K，但类型为 Curie | 温度与原始 953 K 相差 1000 倍；仅凭符号 Tc 不能判为 Curie，标记待核验，不改写原标签 |
| CeMnAsO | 347 K Néel；35 K 自旋重排；7 K 其他转变 | 识别出多个事件，但把 below 7 K 标为 exact；347(1) 的不确定度未在当前最小输出结构中单独表达，原引文保留 |
| EuMnBi2 | Mn 子晶格 315 K Néel | 与所给片段一致；尚未核对本地具体结构与样品条件 |
| Mn4N | 745 K Curie | 与片段一致，但该数值来自论文引言引用的既有结果，需追溯原始测量；不视为新发现 |
| KMnSb，无来源 | 无事件并 abstained=true | 拒绝推断测试通过；不能据此评价总体幻觉率 |

来源及短引文见 [pilot_sources.json](evidence/pilot_sources.json)。上述为助手对同一来源的审查，不是独立人工金标准。试点为有意选择的诊断案例，不能报告总体准确率、召回率或标签污染率。语义检查是保守提示，例如“below TSR=35 K”仍需上下文判断，不机械改成上界。

## 当前边界与下一步

可进入训练的已核验记录：**0**；已核验偏好对：**0**。所有模型建议保持 `training_eligible=false`。当前完成的是 API 接通及首批证据提取验证；30 条完整试点、自动检索闭环和扩散偏好训练尚未完成。

下一步顺序：

1. 补齐其余 24 条试点的原始来源，并补全现有短片段的测量条件、磁序和物相信息；优先处理 Mn2Au、Mn3Ir 与 EuMnBi2 的低温事件。
2. 将温度值、误差、界限、事件类型与子晶格分别记录，针对本轮发现的错误做独立复核；未知项保持未知。
3. 核对 MAGNDATA 原始文献与本地 CIF 的对应关系；按可信结构家族统计可构造的偏好对。相同成分或空间群不足以证明同一家族或同一实验物相。
4. 只有在训练集内确认家族和比较关系充足后，开展固定预算的离线偏好实验。测试集的诊断结果不作为训练偏好。

MAGNDATA 两条直接请求曾遇 TLS 错误，未关闭证书校验；文献全文访问也有受限情况。当前保存的是可获取的摘要/检索片段及其来源层级，未声称已完成全文核验。
