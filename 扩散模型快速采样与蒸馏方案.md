# CGDiT 扩散模型快速采样与蒸馏方案

> 文档状态：方案记录，尚未实施<br>
> 创建日期：2026-08-24<br>
> 当前适用模型：MP-20 Base、CFG 及后续 CrystalPIRL 策略<br>
> 目标：在尽量保持晶体有效性、稳定性、新颖性、多样性和性质控制能力的前提下，减少结构生成阶段的网络调用和墙钟时间

## 1. 核心结论

概率流 ODE 证明了：扩散 SDE 存在一个具有相同边缘分布的确定性连续流。它没有证明现有的局部 score 网络只调用一次，就能精确完成从噪声到晶体的映射。

当前 CGDiT 不能直接套用普通图像扩散模型的一步采样，原因是模型同时处理：

1. 空间群约束下的连续晶格变量；
2. 周期环面上的分数坐标；
3. 吸收 MASK 状态的离散原子类型 D3PM；
4. 独立 Wyckoff 轨道及其对称展开。

因此，推荐路线不是立即追求一步生成，而是：

1. 先实现无需重新训练的 100、50、20 步混合快速采样器；
2. 评估速度和生成质量的 Pareto 前沿；
3. 选出可靠的少步教师采样器；
4. 再将最终 CrystalPIRL 策略蒸馏为 8、4、2、1 步学生模型。

当前正在进行的 CrystalPIRL 强化学习实验继续使用原始采样器，避免中途改变策略定义和实验口径。

## 2. 概率流 ODE 为什么不自动等于一步生成

对于正向 SDE：

$$
d\mathbf{x}
=
\mathbf{f}(\mathbf{x},t)\,dt
+
g(t)\,d\mathbf{w},
$$

对应的概率流 ODE 为：

$$
\frac{d\mathbf{x}}{dt}
=
\mathbf{f}(\mathbf{x},t)
-
\frac{1}{2}g(t)^2
\nabla_{\mathbf{x}}\log p_t(\mathbf{x}).
$$

它与原 SDE 在每个时间点具有相同的边缘分布，可以通过确定性 ODE 将先验分布输运到数据分布。

形式上可以写成：

$$
\mathbf{x}_0
=
\mathbf{x}_T
+
\int_T^0
\mathbf{v}_\theta(\mathbf{x}(t),t)\,dt.
$$

但是，积分中的 $\mathbf{x}(t)$ 本身沿未知轨迹变化。除非速度场有解析解，否则仍然需要多次调用网络进行数值积分。

直接使用一次 Euler 近似：

$$
\mathbf{x}_0
\approx
\mathbf{x}_T
-
T\mathbf{v}_\theta(\mathbf{x}_T,T)
$$

只使用了起点处的局部方向。当概率流轨迹弯曲时，一步 Euler 会产生较大误差。真正的一步生成通常需要另一个模型学习完整流映射或区间平均速度。

参考：

- [Score-Based Generative Modeling through Stochastic Differential Equations](https://arxiv.org/abs/2011.13456)
- [Denoising Diffusion Implicit Models](https://arxiv.org/abs/2010.02502)
- [DPM-Solver](https://arxiv.org/abs/2206.00927)

## 3. 当前 CGDiT 的生成成本

当前 MP-20 Base 配置使用：

```yaml
timesteps: 1000
```

对应文件：`conf/model/exp_mp20_base.yaml`。

采样时实际执行 $999$ 个反向时间步。每个时间步包含：

- 一次 corrector 模型计算；
- 一次 predictor 模型计算。

无条件生成且 `guidance_scale=1.0` 时，每批轨迹约需要：

$$
999\times 2=1998
$$

次 decoder 前向计算。

当 `guidance_scale != 1.0` 时，每次模型计算又包含条件分支和无条件分支，因此真正的 CFG 计算量还会增加。

相关实现位置：

- 采样循环：`cgdit/pl_modules/diffusion.py`
- corrector：`Diffusion.sample()` 中的 corrector 段
- predictor：`Diffusion.sample()` 中的 predictor 段
- CFG 双分支：`Diffusion._get_model_output()`
- 离散原子类型后验：`cgdit/pl_modules/diff_utils/discrete_diff_utils.py`

如果最终改成 predictor-only 的 20 步采样器，去噪网络调用量理论上可由约 1998 次降至 20 次，即约 100 倍 NFE 降低。端到端速度不会达到完整的 100 倍，因为还存在图构建、性质预测、奖励计算和文件写入。

## 4. 可参考的快速扩散方法

| 方法 | 是否需要重新训练 | 典型 NFE | 对 CGDiT 的意义 |
|---|---:|---:|---|
| DDIM | 否 | 20–100 | 可用于构造确定性跨步更新，但需要分别适配晶格、周期坐标和原子类型 |
| DPM-Solver | 否 | 10–20 | 适合连续通道，不能直接处理离散原子类型 |
| Progressive Distillation | 是 | 4–8 | 逐级将教师步数减半，流程较稳但训练阶段较多 |
| Consistency Model | 是 | 1–8 | 可从预训练扩散模型蒸馏直接噪声到数据的映射 |
| Consistency Trajectory Model | 是 | 1–任意 | 学习任意 $t\rightarrow s$ 长跳跃，适合可变步数采样 |
| Shortcut Model | 通常需要新训练 | 1–多步 | 将跳跃长度作为模型输入，一个模型支持不同计算预算 |
| MeanFlow | 通常从头训练 | 1 | 学习区间平均速度，不是现有 CGDiT checkpoint 的直接替换件 |
| Rectified Flow/Reflow | 是 | 1–少步 | 通过拉直概率流轨迹降低一步近似难度 |

主要参考文献：

- [Progressive Distillation for Fast Sampling of Diffusion Models](https://arxiv.org/abs/2202.00512)
- [Consistency Models](https://proceedings.mlr.press/v202/song23a.html)
- [Consistency Trajectory Models](https://arxiv.org/abs/2310.02279)
- [One Step Diffusion via Shortcut Models](https://openreview.net/pdf?id=OlzB6LnXcS)
- [Mean Flows for One-step Generative Modeling](https://arxiv.org/abs/2505.13447)
- [InstaFlow](https://arxiv.org/abs/2309.06380)

## 5. 晶体材料领域的相近工作

目前尚未发现已经成熟验证、可以直接应用于 CGDiT 这种混合离散—连续晶体扩散模型的一步蒸馏方案。最接近的晶体快速生成工作主要通过流匹配或统一表示重新训练生成模型。

### 5.1 FlowMM

FlowMM 在晶体周期流形上使用 Riemannian Flow Matching，显式处理平移、旋转、排列和周期边界条件。它证明了流匹配可以提高晶体生成效率，但不能直接加载现有 CGDiT checkpoint。

- [FlowMM: Generating Materials with Riemannian Flow Matching](https://proceedings.mlr.press/v235/miller24a.html)

### 5.2 CrystalFlow

CrystalFlow 使用条件流匹配，同时生成晶格参数、分数坐标和原子类型，并处理晶体周期对称性。它适合作为后续新架构参考，而不是当前模型的无训练采样器替换。

- [CrystalFlow: a flow-based generative model for crystalline materials](https://www.nature.com/articles/s41467-025-64364-4)

### 5.3 DiffCrysGen

DiffCrysGen 通过统一晶体表示减少多个独立扩散通道带来的复杂度，并报告较快的结构生成速度，但它仍是独立训练的 score-based 模型。

- [DiffCrysGen: a generative diffusion model for accelerated design of inorganic crystalline materials](https://www.nature.com/articles/s41524-026-02147-1)

## 6. CGDiT 的通道级适配判断

### 6.1 晶格通道

晶格在 crystal-family 向量空间中进行连续扩散，并被投影到空间群允许的子空间。

快速采样必须满足：

1. 从时间 $t$ 直接更新到时间 $s<t$；
2. 使用广义 DDIM、ODE 或高阶 solver 系数；
3. 每次跨步后重新执行空间群晶格投影；
4. 不在空间群禁用自由度上引入噪声。

该通道最接近普通连续扩散，因此是 DPM-Solver 或 DDIM 最容易适配的部分。

### 6.2 分数坐标通道

分数坐标位于周期环面上，使用 wrapped-normal 噪声。不能直接把欧氏坐标的 ODE 更新原样应用后再忽略周期性。

快速采样必须满足：

1. 使用任意 $t\rightarrow s$ 的 sigma 跨步公式；
2. 所有距离和监督使用 minimum-image 或 wrapped 距离；
3. 更新后执行模 1 映射；
4. 只更新独立轨道代表坐标，再通过空间群操作展开。

### 6.3 原子类型通道

原子类型采用吸收 MASK 的离散 D3PM，没有普通连续概率流 ODE。

当前实现中的 `sample_and_compute_posterior_q()` 已支持：

```python
step_size > 1
```

因此可以计算跨多个离散时间步的后验。当前采样器仍固定使用 `step_size=1`，需要将其改为当前时间与目标时间之差：

$$
\text{step\_size}=t-s.
$$

这为无需重训的离散原子跨步采样提供了已有基础。

### 6.4 空间群和 Wyckoff 轨道

快速采样不能破坏当前的商空间策略：

- 只在独立 Wyckoff 轨道代表上采样；
- 轨道内等价原子共享原子类型；
- 坐标通过空间群操作展开；
- 晶格始终投影到空间群允许的自由度。

否则即使生成速度提高，也会直接降低结构有效率和对称一致性。

## 7. 第一阶段：无需重训的少步混合采样器

### 7.1 目标

在不修改 checkpoint 和训练目标的情况下，建立以下采样器：

| 编号 | 采样器 | 目的 |
|---|---|---|
| S0 | 原始 999 步 predictor-corrector | 质量和速度基线 |
| S1 | 999 步 predictor-only | 检查删除 corrector 的影响，理论约 2 倍加速 |
| S2 | 100 步 predictor-only 跨步采样 | 低风险少步候选 |
| S3 | 50 步 predictor-only 跨步采样 | 主候选 |
| S4 | 20 步 predictor-only 跨步采样 | 激进候选 |
| S5 | 10 步 predictor-only 跨步采样 | 探索下限，不默认作为最终方案 |

### 7.2 必要实现

只增加与快速采样直接相关的配置和代码：

- `sampling_steps`：实际采样时间点数量；
- `sampling_schedule`：uniform、quadratic 或 log-SNR；
- `corrector_steps`：允许设为 0；
- 晶格任意 $t\rightarrow s$ 更新；
- 周期坐标任意 $t\rightarrow s$ 更新；
- D3PM `step_size=t-s`；
- 对称投影和轨道展开保持不变；
- 记录 NFE、生成时间和峰值显存。

第一阶段不添加一步学生模型，也不改变正在运行的 RL 训练。

### 7.3 公平比较协议

所有采样器使用：

- 同一模型 checkpoint；
- 同一 prompt；
- 同一初始噪声；
- 同一随机数种子；
- 同一结构数量；
- 同一性质预测器和指标代码。

建议 seed=42 初筛，每个候选至少生成相同的 1,000 个结构；确认趋势后再增加其他种子。

### 7.4 评价指标

#### 速度与资源

- 每个结构平均生成时间；
- samples/s；
- decoder NFE；
- 峰值 GPU 显存；
- 性质预测与图构建时间；
- 总端到端墙钟时间。

#### 结构质量

- Composition Validity；
- Structure Validity；
- Uniqueness；
- Novelty；
- Stability；
- S.U.N. rate；
- 组成和结构多样性。

#### 性质控制

- 形成能分布；
- 带隙分布；
- FE+BG 联合分布；
- 目标窗口命中率；
- 与完整采样器的 Wasserstein distance 或其他分布距离。

### 7.5 第一阶段成功标准

优先选择满足以下条件的最少步数：

1. 结构有效性无明显下降；
2. Stability 和 S.U.N. 不出现不可接受退化；
3. Uniqueness、Novelty 和 Diversity 未出现模式坍塌；
4. 条件性质分布仍朝正确方向移动；
5. 相对原始采样器获得明确端到端加速；
6. 结论在固定随机流的配对比较中成立。

具体非劣效阈值应在看到 S0 基线方差后再设定，当前不预先写死。

## 8. 第二阶段：晶体一致性轨迹蒸馏

### 8.1 目标

训练一个学生模型直接预测概率流轨迹上的长跳跃：

$$
F_\phi(\mathbf{z}_t,t,s,\mathbf{c})
\rightarrow
\mathbf{z}_s,
$$

其中：

- $\mathbf{z}_t$ 包含晶格、分数坐标和原子类型；
- $t$ 是起始时间；
- $s$ 是目标时间；
- $\mathbf{c}$ 是性质条件或策略条件。

通过随机采样不同的 $(t,s)$，同一个学生模型支持 8、4、2、1 步生成。

### 8.2 教师选择

按研究目标区分：

- 为了加速 Base：使用冻结 Base 作为教师；
- 为了加速 CFG：使用对应 FE、BG 或 FE+BG CFG 策略作为教师；
- 为了加速最终材料设计：使用完成验证的 CrystalPIRL 策略作为教师。

不同教师不能混用同一个结论。学生只继承教师分布，不能被当作独立的性能提升来源。

### 8.3 蒸馏数据

固定教师模型，生成轨迹对：

$$
(\mathbf{z}_t,t,s,\mathbf{c},\mathbf{z}^{teacher}_s).
$$

为了节约存储，可以只保存随机选择的长跳跃对，不保存全部 1000 个状态。需要覆盖：

- 高噪声到中噪声；
- 中噪声到低噪声；
- 任意时间到终点；
- 多种原子数和空间群；
- FE、BG、FE+BG 条件范围。

### 8.4 通道损失

#### 晶格损失

在空间群允许的独立晶格子空间中计算误差：

$$
\mathcal{L}_{lat}
=
\left\|
P_G(\hat{\mathbf{l}}_s)
-
P_G(\mathbf{l}^{teacher}_s)
\right\|_2^2.
$$

#### 坐标损失

在独立轨道代表上使用周期 minimum-image 距离：

$$
\mathcal{L}_{coord}
=
\sum_i
\left\|
\operatorname{wrap}
(\hat{\mathbf{x}}_{i,s}-\mathbf{x}^{teacher}_{i,s})
\right\|_2^2.
$$

#### 原子类型损失

在独立轨道代表上使用教师类别分布和学生分布的交叉熵或 KL：

$$
\mathcal{L}_{atom}
=
D_{KL}
\left(
p^{teacher}_s
\parallel
p^{student}_s
\right).
$$

#### 一致性损失

要求一次长跳跃和两次短跳跃得到一致结果：

$$
F_\phi(\mathbf{z}_t,t,s)
\approx
F_\phi
\left(
F_\phi(\mathbf{z}_t,t,u),u,s
\right),
\quad t>u>s.
$$

### 8.5 蒸馏顺序

建议按以下顺序推进：

1. 8 步学生；
2. 4 步学生；
3. 2 步学生；
4. 1 步学生。

只有当前阶段满足结构质量和性质控制要求，才继续减少步数。一步不是必须结果；如果 4 步在质量和速度上更合理，应保留 4 步作为最终工程模型。

## 9. 与 CrystalPIRL 和 OrbitPO 的边界

当前 OrbitPO/GRPO 依赖扩散轨迹中的：

- 状态；
- 随机动作；
- transition log-probability；
- 通道—时间信用分配。

若直接替换成一步确定性生成器，原来的轨迹策略空间将发生改变：

1. 1000 步随机策略变成一次映射；
2. 原 transition log-probability 不再存在；
3. OrbitPO 的通道—时间优势无法原样计算；
4. PPO/GRPO 的 clipped ratio 需要重新定义。

因此当前阶段不应让快速学生替换正在训练的 CrystalPIRL 教师。

推荐关系为：

```text
冻结 Base
    ↓
CrystalPIRL / OrbitPO 强化学习
    ↓
经过独立验证的最终策略
    ↓
Consistency / Trajectory Distillation
    ↓
4–8 步快速学生
    ↓
可选的一步学生
```

如果未来希望在少步学生上继续强化学习，需要单独推导少步 transition kernel 和 log-probability，作为新的研究阶段处理。

## 10. 建议的实验表格

| Policy | Sampler | Steps | Decoder NFE | Time/structure | Comp. valid | Struct. valid | Stability | Unique | Novel | Diversity | S.U.N. | FE score | BG score |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| Base | PC | 999 | 1998 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 |
| Base | Predictor only | 999 | 999 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 |
| Base | Hybrid skip | 100 | 100 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 |
| Base | Hybrid skip | 50 | 50 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 |
| Base | Hybrid skip | 20 | 20 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 |
| CrystalPIRL | PC | 999 | 1998 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 |
| CrystalPIRL student | Consistency | 8 | 8 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 |
| CrystalPIRL student | Consistency | 4 | 4 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 |
| CrystalPIRL student | Consistency | 1 | 1 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 |

## 11. 论文中可能形成的附加结论

如果实验成立，可以形成以下证据链：

1. CrystalPIRL 在保持通用晶体质量的情况下改善目标性质；
2. 混合快速采样器在无需重新训练的情况下显著减少 NFE；
3. 晶体一致性轨迹蒸馏进一步将最终策略压缩到 4–8 步；
4. 快速学生仍保持空间群、Wyckoff 轨道和周期几何一致性；
5. 速度提升没有通过牺牲 Stability、Uniqueness、Novelty 或 Diversity 获得；
6. 一步和少步模型之间存在可量化的速度—质量 Pareto 前沿。

建议将快速采样与蒸馏作为 CrystalPIRL 的扩展或补充结果，而不是在当前主体实验完成前改变论文主线。

## 12. 当前待办

- [ ] 保持正在运行的 CrystalPIRL 任务不变
- [ ] 冻结一个 MP-20 Base 采样质量和速度基线
- [ ] 实现 predictor-only 开关
- [ ] 实现统一的跨步时间表
- [ ] 实现晶格 $t\rightarrow s$ 更新
- [ ] 实现周期坐标 $t\rightarrow s$ 更新
- [ ] 启用 D3PM `step_size=t-s`
- [ ] 验证空间群和 Wyckoff 轨道一致性
- [ ] 完成 999、100、50、20、10 步 seed=42 基准
- [ ] 选择第一阶段快速采样器
- [ ] 设计教师轨迹对数据格式
- [ ] 训练 8 步一致性学生
- [ ] 逐步测试 4、2、1 步学生
- [ ] 对最终 CrystalPIRL 策略重复蒸馏与质量验证

## 13. 方案调整记录

| 日期 | 调整内容 | 原因 | 证据或结果 |
|---|---|---|---|
| 2026-08-24 | 创建方案；确定先少步采样、后轨迹蒸馏 | 当前模型为混合离散—连续扩散，不能直接套用一步概率流公式 | 文献分析与当前采样代码审计 |
